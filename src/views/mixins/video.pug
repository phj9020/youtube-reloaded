mixin video(info)
    div
        if info.fileUrl 
            video(src=`/${info.fileUrl}` width=320 height=200)
        h4
            a(href=`/videos/${info.id}`)=info.title
        p=info.description
        ul 
            each hashtag in info.hashtags
                li=hashtag
        small Created At #{info.createdAt.toDateString()}
        p #{info.meta.views} views
        hr